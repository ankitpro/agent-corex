# Minimal dependencies for building the standalone CLI binary with PyInstaller.
# Does NOT include ML/server packages (torch, faiss-cpu, sentence-transformers,
# fastapi, uvicorn) — those are only needed for `retrieve` / `start` commands
# which are not part of the distributed binary's primary use-case.
typer[all]>=0.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
rich>=13.0.0
pyinstaller
